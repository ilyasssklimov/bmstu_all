name: backend

on:
  push:
    branches: [ sem_07 ]
  pull_request:
    branches: [ sem_07 ]

  workflow_dispatch:

jobs:
  testing:
    runs-on: ubuntu-latest

    steps:
      - name: Checking-out repository
        uses: actions/checkout@v2

      - name: Testing by cppcheck
        run: |
          sudo apt install cppcheck
          cppcheck -q --enable=all --inconclusive back/